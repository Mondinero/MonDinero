import React, { useContext } from 'react';
import { NavLink, Outlet } from 'react-router-dom';
import Link from './link.jsx';

function RootLayout() {
  return <div>Well, need SOMETHING here.</div>;
}

export default RootLayout;
